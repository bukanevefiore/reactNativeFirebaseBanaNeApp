import { StyleSheet } from "react-native";

export default StyleSheet.create({
    container: {
        margin: 10,
        paddingLeft: 10,
        backgroundColor: '#e0e0e0',
        borderRadius: 5,
        flexDirection: 'row',
        borderRadius: 10,
    },
    input: {
        flex: 1
    }
})